#include <stdio.h>
int main( )
{
  int a,i=2;
  scanf ("%d", &a);
  While (i<a)
   {
     if (a%i = = 0);
     printf("Itis not a prime");
     break;  
    }
  else
   {
     printf("It is a prime");
     break;
    }
    i++;
    return 0;
}